# Curated package lists

`top_pypi.json` and `top_npm.json` are curated approximations used only for deterministic lookalike checks. They are not official popularity rankings and must not be presented as such.
